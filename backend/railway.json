{
  "//": "Migrations run as preDeployCommand, never from a Procfile `release:` line: Nixpacks compiles `release:` into a build-layer RUN, and at build time the private network does not exist yet, so `postgres.railway.internal` cannot resolve and the build fails.",
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "preDeployCommand": "python manage.py migrate --noinput",
    "startCommand": "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "healthcheckPath": "/api/health/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE"
  }
}
